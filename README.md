# Biarder  
Hello, I am Biarder.  
I am reading **The Practice of Programming** by *Brian W. Kernighan* & *Robert C. Pike*.  

I am currently interested in compilers/interpreters.

Using cwm/nvi on OpenBSD.
And I use Debain Wheezy system for fun.  

# Coding Style
```c
int	fib(int);

int
fib(int n)
{
	if (n <= 1) return (n);
	return (fib(n - 1) + fib(n - 2));
}
```
